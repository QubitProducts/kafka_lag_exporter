blacklist:
  - '^(console-consumer-|python-kafka-consumer-).*$'

kafka:
  local:
    clientID: "kafka-lag-exporter"
    topic: '__consumer_offsets'
    broker:
      - kafka:9092
    zookeeper:
      quorum:
        - zookeeper
      path: '/local'
      offsets: true
      timeout: 10s
